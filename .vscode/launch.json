{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "msedge",
      "name": "Launch",
      "request": "launch",
      "url": "http://localhost:3000",
      "webRoot": "${workspaceFolder}",
      "preLaunchTask": "npm run dev"
    }
  ]
}
